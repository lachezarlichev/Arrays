function maxSequance(array) {
  let result = "";
  let num = 0;
  let currentNumber = 0;
  for (let i = 0; i < array.length; i++) {
    currentNumber = array[i];

  }
  console.log(result);
}
maxSequance([0, 1, 1, 5, 2, 2, 6, 3, 3]);
